# Kiosk demo

Statyczna prezentacja na pełny ekran: `index.html` + kilka plików JS/CSS, bez budowania. Parametr `?p=N` wybiera sekcję startową (0–6).

Klawisze: `1–3` scenariusz · `spacja` pauza · `←/→` scenariusz · `↑/↓` sekcja · `R` od nowa · `S` ustawienia · `F` pełny ekran.
